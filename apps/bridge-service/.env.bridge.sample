# PK Bridge Service minimal configuration
# Copy to apps/bridge-service/.env (or export variables) and adjust as needed.

# Network
SERVER_HOST=0.0.0.0
BRIDGE_SERVICE_PORT=5000

# Manual COM fallback
SCALE_MANUAL_PORT=COM3
SCALE_MANUAL_SCALE_ID=manual-COM3
SCALE_MANUAL_BAUD_RATE=9600
SCALE_MANUAL_DATA_BITS=8
SCALE_MANUAL_PARITY=None
SCALE_MANUAL_STOP_BITS=One
# Optional explicit native device path (Linux/macOS), e.g. /dev/ttyUSB0
SCALE_MANUAL_NATIVE_PORT=

# Database (TFCPILOT3)
SCALE_DB_SERVER=192.168.0.86
SCALE_DB_PORT=49381
SCALE_DB_NAME=TFCPILOT3
SCALE_DB_USERNAME=NSW
SCALE_DB_PASSWORD=B3sp0k3

# General
DEFAULT_SCALE_BAUD_RATE=9600
WEIGHT_POLLING_INTERVAL_MS=400
SCALE_READ_TIMEOUT_MS=500
SCALE_DEFAULT_UNIT=KG
VERBOSE_LOGGING=false
